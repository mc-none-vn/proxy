{
  "name": "automated-serverless-proxy-gateway",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "vercel-build": "npx @vercel/ncc build index.js -o dist"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "socks-proxy-agent": "^8.0.0"
  }
}
